# 开发过程日志

> 记录“竞赛对拍机”从零到当前版本的迭代过程、关键决策与踩坑修复。
> 日期统一按里程碑记录；每个小节含背景、做法与结论。

## 版本总览

| 版本 | 里程碑 | 一句话概括 |
| --- | --- | --- |
| v0.1 | 核心对拍闭环 | 四区块界面 + 内置/外置生成 + 对拍比较 + 失败现场；线程安全重构 |
| v0.2 | 体验补全 | 预览不弹窗、构成主义 UI、数组引用长度（2\*n / n\*m）、无终端子进程 |
| v0.3 | 整窗滚动 | 整窗可滚 + 滚轮翻页、数组多行（行数参数） |
| v0.4 | 结构生成 | 内置新增 排列 / 树 / 图，可被引用 |
| v0.5 | C++ 支持 | .cpp 自动编译、外置生成器编译、试运行 |
| v0.6 | 试运行修复 + 源码查看 | 修“卡在正在试运行”、相对路径、内嵌源码查看 |
| v0.7 | UI 重设计 | Geometric Bold 全量重设计（clam、四色、色块标题栏） |
| v0.8 | 滚轮打磨 | 灵敏度三连：单位累加 → 加大 → 像素级平滑 |
| v0.9 | 滚动链 + 折叠 | 内层到边界滚轮交整窗；区块可折叠、状态跨会话保存 |
| v0.10 | 风格回退 | 回归最朴素的原生主题界面，功能全保留 |

---

## v0.1　初版：核心对拍闭环（tkinter 桌面程序）

**需求**：纯标准库的图形化对拍机，界面直观、跨平台。

- 界面四区块：程序路径 / 数据生成器（内置+外置）/ 对拍参数 / 控制与日志 + 状态栏。
- 内置生成器：整数、浮点数、数组三种变量行，可增删、上下移动；“预览生成示例”弹窗显示。
- 外置生成器：运行命令、stdout 作为输入，种子以 `--seed` 传递。
- 对拍逻辑：每轮 生成数据 → 运行正解/暴力 → 比较输出（严格或忽略行末空格）
  → PASS/WA/TLE/RE；WA 时保存 `test.in / prog.out / std.out` 到 `./fail/`。

**踩坑**：Tk 非线程安全——后台线程直接读 `StringVar`/调 `after` 会抛
`RuntimeError: main thread is not in main loop`。
**修复**：线程模型重构——主线程快照全部配置（Tcl 变量只在主线程读），
后台线程只碰纯 Python 与 subprocess，UI 更新经 `queue.Queue` + 主线程 `after` 轮询回写。

---

## v0.2　预览不弹窗 + 构成主义 UI + 数组引用长度

**需求**：①预览不要弹窗；②前端更像软件、无终端；③输入格式要考虑 2\*n、n\*m。

- 预览改为内置面板内的只读“生成样例预览”区，不再弹 messagebox。
- 顶部海报横幅（黑底、红色斜条纹、大字标题），分区标题加粗着色。
- 数组新增“每行长度”来源：随机范围或**引用前面变量的值**（浮点取整）。
  - `2*n`：行数固定、每行长度引用 n；`n*m` 用两个前置变量分别引用行列。
- Windows 子进程统一加 `CREATE_NO_WINDOW`，杜绝终端闪现。

**踩坑**：引用取值用 `lines[ref_idx]`（按行号）在多行数组存在时错位。
**修复**：改为维护 `produced[i]`（每个配置项生成的行列表）与 `ref_vals[i]`（可引用数值），
引用一律取 `produced[ref_idx][0]` 首行首 token，保证正确。

---

## v0.3　整窗可滚 + 数组多行（行数参数）

**需求**：①整个软件要有大滚轮（整窗可滚+滚轮翻页）；②数组输出多行。

- 全部区块包进外层 Canvas + 右侧滚动条，状态栏固定底部；内容超高显示滚动条、
  不足撑满并自动隐藏；`bind_all` 整页滚轮，自滚动区域（变量列表/日志/预览）不接管。
- 数组新增“行数”来源（随机范围或引用），输出 R 行 × L 元素。

**踩坑**：整窗滚轮与变量列表滚轮双重触发。
**修复**：变量列表滚轮返回 `break` 阻止 `bind_all` 再触发；区域判断用父链遍历。

---

## v0.4　内置生成器新增 排列 / 树 / 图

**需求**：内置生成器可“打包”输出简单数据结构（树、图等）。

- 新增三种类型：
  - 排列：1..n 随机排列，一行输出；
  - 树：`n` + n-1 条边，构造随机带标号树，边打乱；支持边权（无/整数/浮点）；
  - 图：`n m` + m 条边；有向/无向、任意/保证连通、边权；校验 m 不超上限、连通 m≥n-1。
- 树/图/排列的 n、m 可被数组引用，支持“树 n + n 个权值”等组合。

**踩坑**：无向连通图补边时 `(1,2)` 与 `(2,1)` 作为不同元组进入 set 造成重复边。
**修复**：无向边统一归一化 `(min,max)` 后再入集合。

---

## v0.5　C++ 源码自动编译 + 外置生成器编译 + 试运行

**需求**：正解/暴力都是 C++；要能直接传入 .cpp 自动编译运行。

- 程序路径区新增“运行命令 / C++ 源码”模式下拉，浏览 `.cpp/.cc/.cxx/.c` 自动识别；
  新增“编译设置”（编译器默认 g++、参数默认 `-O2 -std=c++17`）。
- `_compile_cpp`：`g++ <参数> <源码> -o <临时目录>/<name>.exe`，cwd=源码目录、
  `CREATE_NO_WINDOW`、超时 60s；编译失败输出 stderr 并停止（reason=编译失败）。
- 外置生成器同样支持 C++ 自动编译，每轮运行并追加 `--seed`。
- 新增试运行：正解/暴力各自按钮 + 独立“试运行输出”区（后台线程生成样例→编译→运行→回显）。

**用户反馈**：WinError 193（.cpp 被直接当可执行文件运行）——因上一版本只规划未实现编译。
**结论**：本版本落实自动编译后问题消除。

---

## v0.6　试运行修复 + 内嵌源码查看

**需求**：试运行点下去“没反应/卡在正在试运行”；软件里能看到传入 cpp 的源码。

- **试运行修复**：
  - 相对路径 bug：C++ 源码相对路径（`./sol.cpp`）未基于程序目录解析，导致“找不到源码文件”；
    改为快照时按程序目录转绝对路径（对拍路径一并修复）。
  - 永不静默失败：worker 整体 try/except，任何异常回显到试运行输出区；
  - 分阶段进度提示（正在生成样例/编译/运行），不再误判卡死；
  - 写入结果后自动将试运行区滚入视野。
- **内嵌源码查看**：新增“源码查看”区（黑底标题栏 + 行号 + 只读等宽文本），
  程序行与生成器面板各加“源码”按钮；二进制/缺失文件有明确提示。

**踩坑**：源码含 CRLF 时 Tk 按 `\n` 换行导致行号错位。
**修复**：载入前统一 `\r\n→\n`、去除 UTF-8 BOM，行号按 `split("\n")` 计算。

---

## v0.7　Geometric Bold 全量 UI 重设计

**需求**：按“Geometric Bold（包豪斯/构成主义）”规范重做界面。

- 强制 ttk `clam` 主题，四色：黑 `#000000`、白 `#ffffff`、红 `#ff0000`、蓝 `#0000ff`；
  无渐变、无阴影、无圆角、无超 4 色。
- 自定义区块标题栏：黑底横条 + 色块装饰 + 白粗体标题，左右交替非对称。
- 海报横幅：黑底超大标题 + 红圆、蓝菱形（旋转）、红色三角、斜白线。
- 按钮纯色块（黑/红/蓝），hover 即时反色、按下压平；输入框白底黑边、focus 变红
  （键盘焦点可见）；类型标签红/蓝色块；滚动条黑块白轨。

---

## v0.8　滚轮灵敏度与平滑度迭代

**需求演变**：不灵敏 → 还是不够灵敏 → 卡卡的。

- **v0.8a**：`yview_scroll` 只接受整数，改为 `_wheel_units` + `_scroll_units`
  （浮点累积、整单位滚动），并提升每格距离（变量列表 1→3 单位、整窗 1→4 单位）。
- **v0.8b**：用户仍觉不够灵敏，继续提升（变量列表 3→6 单位、整窗 4→8 单位）。
- **v0.8c（本版）**：用户反馈“卡卡的”——根因是 24px 量化步长导致平滑滚轮一顿一顿。
  **修复**：`yscrollincrement=24 → 1`（1 单位 = 1px），`_wheel_units` 改为按像素换算
  （`-delta/120 * PX`）：变量列表每格 120px、整窗每格 160px；
  平滑滚轮小步长（delta 10/20/30）逐像素推进，不再 24px 顿跳；钳制与 `break` 保留。

---

## v0.9　滚动链 + 区块折叠

**需求**：滚轮“卡卡的”真正根因——鼠标在内层自滚动区域（变量列表/日志/预览/源码）时，
外层整窗滚轮被禁用，内层没得滚时轮子像“失灵”，造成卡顿错觉。

- **滚动链**：内层自滚动区域滚动到边界时，把滚轮事件交给外层整窗（`_on_canvas_wheel`
  到边界返回 `None` 放行 `bind_all`；`_on_window_wheel` 用 `_region_at_boundary` 判断
  是否接管）；内层还有空间时仍由它自己滚，避免双重滚动。
- **区块折叠**：5 个主区块标题栏可点击折叠/展开（箭头 −/＋），`body_inner` 尺寸变化
  自动重算整窗滚动条；折叠状态跨会话保存到 `duipai_state.json`（程序/exe 目录），
  启动时恢复，读写失败静默。

---

## v0.10　风格回退：朴素版

**需求**：把界面风格改回最开始最朴素的版本。

- `_setup_style` 恢复自适应原生主题（Windows vista / macOS aqua / Linux clam），
  朴素浅色（`accent=#2f6fed`、`panel_bg=#f4f6fa`、`status_bg=#2b3440`），
  默认控件外观；移除色块按钮/标签/滚动条、粗黑边框等 Geometric Bold 样式。
- 移除海报横幅；区块标题栏改为浅色朴素横条；状态栏去掉红方块；变量卡片回到
  `ridge bd=1`；文本区细边框。
- 功能全部保留：区块折叠（跨会话状态）、滚动链、源码查看、试运行、C++ 编译等。
- 未定义的 `Blue/Red/Square.TButton` 样式由 ttk 自动回落到 `TButton`，无残留引用。

---

## 其它工程性工作

- **打包**：`build.bat` / `build.sh`（PyInstaller `--noconsole --onefile`）；
  图标由 `make_icon.py` 纯标准库生成（PNG + 多尺寸 ICO，RGBA）。
- **文档**：本日志 + `README.md`。
- **测试**：每个里程碑均以临时脚本做端到端验证（真实 g++ 编译、WA/TLE/RE、
  结构生成、滚轮钳制/平滑、试运行、源码查看、滚动链、折叠持久化），
  通过后重建 exe 并启动冒烟。

---

## 当前功能清单（截至 v0.9）

- **程序**：运行命令 / C++ 源码两种模式；C++ 自动编译（g++，可配编译器与参数）；
  相对路径按程序目录解析。
- **生成器**：内置 整数/浮点/数组/排列/树/图，数组多行（2\*n、n\*m），
  树/图边权与有向/连通选项；外置生成程序（含 C++），`--seed` 传种子；
  各来源支持“引用前面变量”。
- **验证**：生成样例预览、正解/暴力试运行（独立输出区）。
- **对拍**：组数/超时/种子/忽略行末空格；PASS/WA/TLE/RE；WA 保存现场到 `./fail/`。
- **UI**：朴素自适应主题（原生控件外观）；整窗可滚 + 像素级平滑滚轮 + 滚动链；
  5 个区块可折叠，状态跨会话保存（`duipai_state.json`）；源码查看（行号）。
- **工程**：纯标准库；后台线程 + 消息队列；Windows 无终端子进程；打包脚本与图标生成。

## 可能的后续方向

- 对拍结果图表/统计视图；失败样例一键重跑与定位。
- 生成器配置模板保存/载入（多组常用数据形态）。
- 更多结构类型（如括号序列、字符串、排列+树的复合）与自定义格式模板。
